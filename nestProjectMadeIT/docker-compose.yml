version: '3.8'

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"   # code-server (افتحه بالمتصفح)
      - "3000:3000"   # NestJS app
    volumes:
      - ./project:/home/coder/project
